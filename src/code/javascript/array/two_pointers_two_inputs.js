let fn = (arr1, arr2) => {
    let ans = 0
    let i = 0
    let j = 0

    while (i < arr1.length && j < arr2.length) {
        // do some logic here
        if (CONDITION) {
            i++
        } else {
            j++
        }
    }

    while (i < arr1.length) {
        // do logic
        i++
    }

    while (j < arr2.length) {
        // do logic
        j++
    }

    return ans
}
