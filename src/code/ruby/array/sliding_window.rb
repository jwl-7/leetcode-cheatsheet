def fn(arr)
  n       = arr.length
  window  = 0
  left    = 0
  result  = 0

  (0...n).each do |right|
    # TODO: add arr[right] to window

    while WINDOW_CONDITION_BROKEN
      # TODO: remove arr[left] from window
      left += 1
    end

    # TODO: update result
  end

  result
end
